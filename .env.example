DUNE_API_KEY=your_key_here

# NEON (or any Postgres) - optional. When set, Streamlit loads main data from DB instead of CSV/Supabase.
# Get from NEON dashboard: Connection string → "Pooled connection" (e.g. postgresql://user:pass@ep-xxx.us-east-2.aws.neon.tech/neondb?sslmode=require)
# DATABASE_URL=postgresql://user:password@host/dbname?sslmode=require
