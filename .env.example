DUNE_API_KEY=your_key_here

# NEON (or any Postgres) - optional. When set, Streamlit loads main data from DB instead of CSV/Supabase.
# Get from NEON dashboard: Connection string → "Pooled connection"
# DATABASE_URL=postgresql://user:password@host/dbname?sslmode=require
# If you loaded data into a different table (e.g. with psql \copy), set its name:
# NEON_TABLE=balancer_data
